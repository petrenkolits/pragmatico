import org.bson.Document
import pragmatico.Project

model {
  Map result
}

json {
//  results g.render(result.projects, [includes: ['id', 'name', 'status']])
  results {
    result.projects.each { Document doc ->
      project(xxx: doc.get("_id").toString())
    }
  }
  metadata result.metadata
}

